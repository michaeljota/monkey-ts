{
  "name": "monkey",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build --compile --minify --sourcemap --bytecode ./index.ts --outfile ./bin/monkey"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.4.2"
  },
  "peerDependencies": {
    "typescript": "5.7.3"
  }
}
